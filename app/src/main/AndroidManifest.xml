<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.keepsake">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".ChangeFamily" />
        <activity android:name=".UpdateDetails" />
        <activity android:name=".FamilyMemberPage" />
        <activity
            android:name=".ViewFamilyItems"
            android:label="@string/title_activity_view_family_items"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SignUpPage" />
        <activity android:name=".RequestFamilyGroup" />
        <activity android:name=".JoinFamilyGroup" />
        <activity android:name=".CreateFamilyGroup" />
        <activity android:name=".NewItemUpload" />
        <activity android:name=".AccountSettings" />
        <activity android:name=".HomePage" />
        <activity android:name=".FamilySetup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>