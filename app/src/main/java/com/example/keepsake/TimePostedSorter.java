package com.example.keepsake;

import java.util.Comparator;

public class TimePostedSorter implements Comparator<Items> {
    @Override
    public int compare(Items item1, Items item2) {
//        return item1.getTimePosted().compareTo(item2.getTimePosted);
//        then use arraylist.sort(new TimePostedSorter());
        return 0;
    }
}
